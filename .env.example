# Database connection string
DATABASE_URL=postgres://user:password@localhost:5432/equilibragem_db

# JWT secret key - generate a secure one with: openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_here

# Server port (optional, defaults to 3000)
PORT=3000